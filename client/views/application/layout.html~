<template name="layout">

        <div class="container-fluid">
            {{>header}}
        </div>
        <div class="container-fluid">
            {{yield}}
        </div>
    </div>
</template>